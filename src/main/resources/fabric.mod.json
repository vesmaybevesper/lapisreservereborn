{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "authors": ["${authors}"],
  "contributors": ["${contributors}"],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}",
    "homepage": "${homepage_url}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}"
      }
    }
  },
  "description": "${description}",
  "icon": "assets/icon.png",
  "license": "${license}",
  "environment": "*",
  "accessWidener": "aw/26.1.0.accesswidener",
  "entrypoints": {
    "main": [
      "dev.vesper.lapisreservereborn.platform.fabric.FabricEntrypoint"
    ],
    "client": [
      "dev.vesper.lapisreservereborn.platform.fabric.FabricClientEntrypoint"
    ]
  },
  "mixins": [
    "lapisreservereborn.mixins.json"
  ]
}
